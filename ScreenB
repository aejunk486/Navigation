import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function ScreenB({ route }) {
  const { itemId, message } = route.params;
  var number = "";
  
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Screen2</Text>
      <Text style={styles.text}>Item ID: {itemId}</Text>
      <Text style={styles.text}>Status: {number}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1, alignItems: 'center', justifyContent: 'center',
  },
  title: {
    fontSize: 24, marginBottom: 10,
  },
  text: {
    fontSize: 18, marginVertical: 4,
  },
});
